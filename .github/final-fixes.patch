diff --git a/apps/backend/src/modules/pedidos/pedido-cancelacion.repository.ts b/apps/backend/src/modules/pedidos/pedido-cancelacion.repository.ts
--- a/apps/backend/src/modules/pedidos/pedido-cancelacion.repository.ts
+++ b/apps/backend/src/modules/pedidos/pedido-cancelacion.repository.ts
@@ -14,7 +14,7 @@ export interface CancellationRepositoryRow {
   id: string;
   pedido_id: string;
   numero_pedido: string;
   comprador_id: string;
-  email_comprador: string;
+  email_comprador: string | null;
   estado: CancellationState;
   motivo: string;
   importe: string;
@@ -24,6 +24,10 @@ export interface CancellationRepositoryRow {
   pago_id: string;
   stripe_payment_intent_id: string | null;
   stripe_checkout_session_id: string | null;
+  reembolso_stripe_id?: string | null;
+  reembolso_estado?: StripeRefundStatus | null;
+  reembolso_error_code?: string | null;
+  reembolso_ultima_comprobacion_at?: Date | string | null;
 }
 
 export interface CancellationLine {
diff --git a/apps/backend/test/pedido-cancelacion.spec.ts b/apps/backend/test/pedido-cancelacion.spec.ts
--- a/apps/backend/test/pedido-cancelacion.spec.ts
+++ b/apps/backend/test/pedido-cancelacion.spec.ts
@@ -290,7 +290,7 @@ describe("PedidoCancelacionService", () => {
     expect(payload.recipient).toBe("buyer@example.com");
     expect(payload.template).toBe("pedido_cancelado");
     expect(payload.reference).toBe("pedido-1");
-    expect(payload.data[0]?.key).toBe("pedido_id");
+    expect((payload.data[0] as { key: string; value: string }).key).toBe("pedido_id");
     expect(payload.data[0]?.value).toBe("pedido-1");
   });
 });

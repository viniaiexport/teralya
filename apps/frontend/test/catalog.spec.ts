import { describe,expect,it,vi } from 'vitest';
import { buildCatalogPath,catalogFiltersFromSearch,catalogHref,formatEuro,type WineSummary } from '../src/lib/api/catalog';
vi.mock('server-only',()=>({}));
describe('cliente público de catálogo',()=>{it('crea una URL codificada y omite vacíos y defaults',()=>{expect(buildCatalogPath({q:'Rioja & Ribera',region:' ',page:1,page_size:20,precio_min:'10.00'})).toBe('/vinos?q=Rioja+%26+Ribera&precio_min=10.00')});it('normaliza la URL sin propagar filtros inválidos',()=>{expect(catalogFiltersFromSearch({q:'  reserva  ',page:'-2',page_size:'101',precio_min:'10',precio_max:'20.00'})).toEqual({q:'reserva',precio_max:'20.00'})});it('mantiene filtros al paginar',()=>{expect(catalogHref({tipo_vino:'tinto',page:2},3)).toBe('/vinos?tipo_vino=tinto&page=3')});it('formatea decimales como euros',()=>{expect(formatEuro('10.50')).toMatch(/10,50\s€/)});it('modela campos públicos sin exigir opcionales',()=>{const wine={id:'id',nombre_comercial:'Reserva',precio:'10.00',moneda:'EUR',disponible_venta:true,bodega:{id:'bodega',nombre_comercial:'Origen'}} satisfies WineSummary;expect(wine.bodega.nombre_comercial).toBe('Origen');expect(wine).not.toHaveProperty('stock_disponible')})});

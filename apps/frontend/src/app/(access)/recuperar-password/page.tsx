import { RecoveryForm } from '../auth-forms';
export default function RecoveryPage(){return <><p className="eyebrow">Recuperación</p><h1>Recupera tu acceso</h1><p className="auth-intro">Te enviaremos instrucciones si el correo está asociado a una cuenta.</p><RecoveryForm/></>}

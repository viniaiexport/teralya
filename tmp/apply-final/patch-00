diff --git a/.env.staging.example b/.env.staging.example
index 7c110a7..b387464 100644
--- a/.env.staging.example
+++ b/.env.staging.example
@@ -29,7 +29,7 @@ STRIPE_CHECKOUT_TTL_SECONDS=1800
 
 # Valores provisionales exclusivos de staging; producción queda bloqueada hasta validación legal.
 MINIMUM_PURCHASE_AGE=18
-ALCOHOL_TERMS_VERSION=staging-not-for-production
+ALCOHOL_TERMS_VERSION=LEGAL-06-v1.0
 
 # Valores provisionales exclusivos de staging; producción requiere aprobación de Seguridad.
 LOGIN_RATE_LIMIT_MAX_ATTEMPTS=5
diff --git a/.github/workflows/backend.yml b/.github/workflows/backend.yml
index b9d75de..fb02ef9 100644
--- a/.github/workflows/backend.yml
+++ b/.github/workflows/backend.yml
@@ -5,8 +5,10 @@ on:
     paths:
       - "apps/backend/**"
       - "docs/INF/openapi/**"
-      - "docs/INF/teralya_schema_v1.4_APROBADO.sql"
+      - "docs/INF/teralya_schema_v1.5_APROBADO.sql"
+      - "scripts/generate_openapi_contract.py"
       - "scripts/validate_openapi_contract.py"
+      - "database/migrations/**"
       - "package.json"
       - "package-lock.json"
       - ".github/workflows/backend.yml"
@@ -15,8 +17,10 @@ on:
     paths:
       - "apps/backend/**"
       - "docs/INF/openapi/**"
-      - "docs/INF/teralya_schema_v1.4_APROBADO.sql"
+      - "docs/INF/teralya_schema_v1.5_APROBADO.sql"
+      - "scripts/generate_openapi_contract.py"
       - "scripts/validate_openapi_contract.py"
+      - "database/migrations/**"
       - "package.json"
       - "package-lock.json"
       - ".github/workflows/backend.yml"
@@ -92,11 +96,17 @@ jobs:
           cache: npm
       - run: npm ci
       - name: Apply approved PostgreSQL schema
-        run: psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f docs/INF/teralya_schema_v1.4_APROBADO.sql
+        run: psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f docs/INF/teralya_schema_v1.5_APROBADO.sql
+      - name: Reapply incremental migration idempotently
+        run: psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f database/migrations/20260717_001_cierre_web.sql
       - run: npm run backend:check
       - uses: actions/setup-python@v5
         with:
           python-version: "3.12"
       - run: python -m pip install --disable-pip-version-check pyyaml
-      - run: python scripts/validate_openapi_contract.py docs/INF/openapi/teralya-openapi-v1.0.yaml
-      - run: npx --yes @redocly/cli lint docs/INF/openapi/teralya-openapi-v1.0.yaml
+      - name: Regenerate and verify the normative OpenAPI contract
+        run: |
+          python scripts/generate_openapi_contract.py
+          git diff --exit-code -- docs/INF/openapi/teralya-openapi-v1.1.yaml
+      - run: python scripts/validate_openapi_contract.py docs/INF/openapi/teralya-openapi-v1.1.yaml
+      - run: npx --yes @redocly/cli lint docs/INF/openapi/teralya-openapi-v1.1.yaml
diff --git a/.github/workflows/deploy-production.yml b/.github/workflows/deploy-production.yml
index d361e12..bc33072 100644
--- a/.github/workflows/deploy-production.yml
+++ b/.github/workflows/deploy-production.yml
@@ -94,7 +94,7 @@ jobs:
           sed -i '/^FRONTEND_IMAGE=/d;/^BACKEND_IMAGE=/d' /tmp/.env.production
           printf '\nFRONTEND_IMAGE=%s\nBACKEND_IMAGE=%s\n' "$FRONTEND_IMAGE" "$BACKEND_IMAGE" >> /tmp/.env.production
           chmod 0600 /tmp/.env.production
-          tar -czf /tmp/teralya-release.tgz deploy/production docs/INF/teralya_schema_v1.4_APROBADO.sql
+          tar -czf /tmp/teralya-release.tgz deploy/production docs/INF/teralya_schema_v1.5_APROBADO.sql database/migrations/20260717_001_cierre_web.sql
 
       - name: Authenticate the server to GHCR
         shell: bash
@@ -115,9 +115,10 @@ jobs:
             rm -rf /tmp/teralya-release
             mkdir -p /tmp/teralya-release
             tar -xzf /tmp/teralya-release.tgz -C /tmp/teralya-release
-            sudo install -d -o deployer -g deployer -m 0750 /srv/teralya /srv/teralya/docs/INF
+            sudo install -d -o deployer -g deployer -m 0750 /srv/teralya /srv/teralya/docs/INF /srv/teralya/database/migrations
             sudo cp -a /tmp/teralya-release/deploy/production/. /srv/teralya/
-            sudo install -o deployer -g deployer -m 0644 /tmp/teralya-release/docs/INF/teralya_schema_v1.4_APROBADO.sql /srv/teralya/docs/INF/
+            sudo install -o deployer -g deployer -m 0644 /tmp/teralya-release/docs/INF/teralya_schema_v1.5_APROBADO.sql /srv/teralya/docs/INF/
+            sudo install -o deployer -g deployer -m 0644 /tmp/teralya-release/database/migrations/20260717_001_cierre_web.sql /srv/teralya/database/migrations/
             sudo install -o deployer -g deployer -m 0600 /tmp/.env.production /srv/teralya/.env.production
             sudo chown -R deployer:deployer /srv/teralya
             chmod 0750 /srv/teralya/scripts/*.sh
diff --git a/.github/workflows/staging.yml b/.github/workflows/staging.yml
index 85bdc25..a0d5cc7 100644
--- a/.github/workflows/staging.yml
+++ b/.github/workflows/staging.yml
@@ -11,6 +11,8 @@ on:
       - ".dockerignore"
       - ".env.staging.example"
       - "compose.staging.yaml"
+      - "database/migrations/**"
+      - "docs/INF/teralya_schema_v1.5_APROBADO.sql"
       - ".github/workflows/staging.yml"
   push:
     branches: [main]
@@ -23,6 +25,8 @@ on:
       - ".dockerignore"
       - ".env.staging.example"
       - "compose.staging.yaml"
+      - "database/migrations/**"
+      - "docs/INF/teralya_schema_v1.5_APROBADO.sql"
       - ".github/workflows/staging.yml"
   workflow_dispatch:
 
@@ -102,10 +106,11 @@ jobs:
           SSH_HOST: ${{ secrets.STAGING_SSH_HOST }}
           SSH_USER: ${{ secrets.STAGING_SSH_USER }}
         run: |
-          ssh -i ~/.ssh/teralya_staging "$SSH_USER@$SSH_HOST" 'install -d -m 750 ~/teralya/infrastructure/staging ~/teralya/docs/INF'
+          ssh -i ~/.ssh/teralya_staging "$SSH_USER@$SSH_HOST" 'install -d -m 750 ~/teralya/infrastructure/staging ~/teralya/docs/INF ~/teralya/database/migrations'
           scp -i ~/.ssh/teralya_staging compose.staging.yaml "$SSH_USER@$SSH_HOST:~/teralya/compose.staging.yaml"
           scp -i ~/.ssh/teralya_staging infrastructure/staging/Caddyfile "$SSH_USER@$SSH_HOST:~/teralya/infrastructure/staging/Caddyfile"
-          scp -i ~/.ssh/teralya_staging docs/INF/teralya_schema_v1.4_APROBADO.sql "$SSH_USER@$SSH_HOST:~/teralya/docs/INF/teralya_schema_v1.4_APROBADO.sql"
+          scp -i ~/.ssh/teralya_staging docs/INF/teralya_schema_v1.5_APROBADO.sql "$SSH_USER@$SSH_HOST:~/teralya/docs/INF/teralya_schema_v1.5_APROBADO.sql"
+          scp -i ~/.ssh/teralya_staging database/migrations/20260717_001_cierre_web.sql "$SSH_USER@$SSH_HOST:~/teralya/database/migrations/20260717_001_cierre_web.sql"
           scp -i ~/.ssh/teralya_staging infrastructure/staging/deploy-staging.sh "$SSH_USER@$SSH_HOST:~/teralya/deploy-staging.sh"
       - name: Install environment and deploy
         env:
diff --git a/apps/backend/README.md b/apps/backend/README.md
index a05614f..1186f1b 100644
--- a/apps/backend/README.md
+++ b/apps/backend/README.md
@@ -1,7 +1,7 @@
 # Backend de Teralya
 
 Monolito modular Node.js/TypeScript desarrollado contract-first. El contrato normativo está en
-`docs/INF/openapi/teralya-openapi-v1.0.yaml`.
+`docs/INF/openapi/teralya-openapi-v1.1.yaml`.
 
 ## Requisitos
 
@@ -17,7 +17,7 @@ cp apps/backend/.env.example apps/backend/.env
 set -a
 . apps/backend/.env
 set +a
-psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f docs/INF/teralya_schema_v1.4_APROBADO.sql
+psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f docs/INF/teralya_schema_v1.5_APROBADO.sql
 npm run start:dev --workspace @teralya/backend
 ```
 
@@ -32,7 +32,7 @@ Los valores de `.env.example` reflejan la decisión vigente y son válidos tambi
 
 ```bash
 npm run backend:check
-python3 scripts/validate_openapi_contract.py docs/INF/openapi/teralya-openapi-v1.0.yaml
+python3 scripts/validate_openapi_contract.py docs/INF/openapi/teralya-openapi-v1.1.yaml
 ```
 
 Las rutas operativas no forman parte del contrato de negocio OpenAPI:
diff --git a/